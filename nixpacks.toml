providers = ['...', 'go']

[variables]
CGO_ENABLED = '1'

[phases.setup]
nixPkgs = [
    'gcc'
]

[phases.install]
cmds = [
    'go install github.com/a-h/templ/cmd/templ@latest',
    'go generate .',
    'go get .'
]

[phases.build]
cmds = [
    'go build .'
]

# [start]
# cmd = ''
